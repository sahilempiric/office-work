from django.contrib import admin

# Register your models here.
# from .models import users, orders, already_like_follow, purchase_coins, user_orders, app_data, coin_details, check_ip_addresses

# class users(admin.ModelAdmin):
#     # model = users
#     list_display = (field.name for field in users._meta.get_fields())

# class orders(admin.ModelAdmin):
#     model = orders
#     list_display = "__all__"

# class already_like_follow(admin.ModelAdmin):
#     model = already_like_follow
#     list_display = "__all__"

# class purchase_coins(admin.ModelAdmin):
#     model = purchase_coins
#     list_display = "__all__"

# class user_orders(admin.ModelAdmin):
#     model = user_orders
#     list_display = "__all__"

# class app_data(admin.ModelAdmin):
#     model = app_data
#     list_display = "__all__"

# class coin_details(admin.ModelAdmin):
#     model = coin_details
#     list_display = "__all__"

# class check_ip_addresses(admin.ModelAdmin):
#     model = check_ip_addresses
#     list_display = "__all__"